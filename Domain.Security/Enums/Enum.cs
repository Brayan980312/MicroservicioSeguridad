namespace Domain.Security.Enums
{
    public class Enum
    {
        /// <summary>Representa los roles existentes en el sistema.</summary>
        public enum Roles
        {
            Administrador = 1,
            Cliente = 2,
        }
    }
}